specVersion: "v1beta"
serviceId: "html files"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "html files-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
